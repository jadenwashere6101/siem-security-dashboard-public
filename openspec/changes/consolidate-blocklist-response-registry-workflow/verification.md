# Mac Phase Verification — consolidate-blocklist-response-registry-workflow

## Dark theme / keyboard / copy / viewport (code + automated)

| Check | Result |
| --- | --- |
| Dark theme | Registry/Blocklist panels retain `#0d1117` / `#161b22` / `#e6edf3` surfaces |
| Keyboard / focus | Tabs and Remove Tracking are native buttons with titles; disabled state uses `aria-disabled` / `disabled` |
| Explanatory copy | Subtitle + Remove Tracking `title` state no-firewall; inactive rows show history-preserved reasons |
| Narrow viewport | Existing `auto-fit` grids and table `overflowX: auto` unchanged |
| Role gating | Viewers: locked copy, no Remove Tracking control |

Screenshots: not captured in this Mac pass (headless). Live visual smoke deferred to authorized VM deploy.
