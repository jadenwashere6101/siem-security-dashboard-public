## 1. Simulation Backend Foundation — Mac AI

- [x] 1.1 Design and document the simulation endpoint contract: request schema (source, raw input text or JSON events, existing rule identifier), response schema (per-stage results, alert preview, MITRE preview, SOAR preview, disclosure flags), and error responses, consistent with `specs/detection-simulator-workspace/spec.md`.
- [x] 1.2 Add a new route module/blueprint exposing `POST /detection-simulator/run` (or equivalent), protected by `login_required` and `analyst_or_super_admin_required`, matching the existing Source Health authorization boundary.
- [x] 1.3 Implement the rollback-only transaction orchestrator: open one connection/transaction, call engine-layer functions directly (never the production `/ingest` route handlers), and guarantee `rollback()` runs on every success and failure path via `try/finally`.
- [x] 1.4 Reuse existing parser/normalizer functions (`adapters/*.py`, `routes/ingest_routes.py` honeypot normalizer) to convert pasted raw log lines or JSON events into normalized event dicts; report parser-stage failures with the parser's own failure reason without proceeding to later stages.
- [x] 1.5 Add a stub/short-circuit for `core/ip_helpers.lookup_ip_reputation` scoped to the simulation code path only, returning a clearly-labeled simulated value and never issuing a live third-party API call; verify production `/ingest` behavior for this function is unaffected. `lookup_ip_location` is never called by the simulator at all (no geo enrichment step is invoked), which trivially satisfies "no live geolocation call" without needing a stub.
- [x] 1.6 **Resolved: no `audit_log` write, explicitly, by design.** `design.md`'s prior open question ("should the simulator write one narrow audit_log entry?") is closed in favor of the strictest safe reading of `spec.md`'s zero-durable-write contract: the simulator never calls `core/audit_helpers.log_audit_event` or writes to `audit_log` for any reason, including a "simulation was run" meta-entry. `spec.md`'s "Zero durable production writes" requirement now names `audit_log` explicitly and has a dedicated scenario. Verified by `test_zero_durable_writes_across_all_guarded_tables` (asserts `audit_log` row count unchanged).
- [x] 1.7 Add an integration test asserting that row counts in `events`, `alerts`, `playbook_executions`, `soar_response_decisions`, `response_actions_queue`, `incidents`, `incident_alerts`, and `audit_log` are unchanged before/after a simulation run that would, on the real `/ingest` path, produce an alert and a matched playbook. Implemented as `tests/test_detection_simulator.py::test_zero_durable_writes_across_all_guarded_tables`, plus `test_mid_pipeline_exception_still_rolls_back` and `test_never_commits_even_when_wrapper_forbids_commit` for the exception-path and no-commit-method guarantees specifically.
- [x] 1.8 Add authentication/authorization contract tests: authorized analyst/super-admin succeeds, unauthenticated request is rejected, insufficient role is rejected, none execute any pipeline stage on rejection.

## 2. Detection Engine Integration — Mac AI

- [x] 2.1 Wire the rollback-only transaction orchestrator to call `engines/ingest_engine.ingest_normalized_event` with the normalized simulated event, exactly as production does, and capture its `alerts_created` return value.
- [x] 2.2 Verify Detection Applicability behavior is unchanged: a rule not applicable to the selected source (per `engines/detection_applicability.py`) is reported as not applicable without executing that rule's detector.
- [x] 2.3 Verify that rule configuration (thresholds, windows, active state from `engines/detection_config.py`) is read live on every simulation run with no simulator-specific configuration copy.
- [x] 2.4 Confirm alert preview output includes the same fields production alert rows would contain (severity, response_action, message, reputation/location placeholders per the stub from task 1.5).
- [x] 2.5 Confirm MITRE mapping (`helpers/enrichment_helpers.enrich_alert_with_mitre`) is applied to the simulated alert preview unmodified.
- [x] 2.6 **Resolved, without modifying detector SQL.** `engines/detection_simulator.py` maps each Version-1 rule id to its exact, unmodified production detector function (`RULE_ID_TO_DETECTOR`) and, only when the real evaluation did not match and was not dedup-suppressed, re-invokes that same function once more on the same transaction with its `threshold` parameter temporarily lowered to the minimum valid value. Every detector already returns its observed count/condition value as a field on its result dict, so this reveals the true observed value using the real query, real window, and real rule configuration. **Zero lines changed in `engines/detection_engine.py` or `engines/correlation_engine.py`** (confirmed via `git diff` — empty). See `design.md`'s "Explainability near-miss evidence" decision for the full design and its disclosed limitation (dedup-suppressed matches don't get a numeric value, since the evidence call hits the same dedup guard).
- [x] 2.7 **Resolved: regression proof is that the existing detection-engine test suite requires zero changes.** Since no detector file was modified, there is no "before/after" instrumentation diff to regress-test — production alerting behavior is unchanged by construction. Ran the full existing suite (`tests/test_failed_login_detection.py`, `test_password_spraying_detection.py`, `test_port_scan_detection.py`, `test_high_request_rate_detection.py`, `test_http_error_detection.py`, `test_application_exception_detection.py`, `test_honeypot_event_detections.py`, `test_pfsense_firewall_detections_soar.py`, `test_source_aware_detection.py`, `test_ingest_normalized_event.py`, and the full suite) unchanged — all pass. Added `test_rule_id_to_detector_maps_to_the_exact_production_functions` (asserts the mapped callables are the identical function objects, not copies) and `test_evidence_call_never_survives_rollback` (proves the evidence call's own phantom alert insert never becomes durable).
- [x] 2.8 Add tests for the existing-open-alert dedup disclosure: a simulated event that would satisfy a threshold but whose source IP already has a real open alert of that type is reported as suppressed, not as a plain non-match. Implemented via a read-only pre-check in the orchestrator (no detector SQL modification) plus `test_existing_open_alert_suppression_is_disclosed`.
- [x] 2.9 Add tests for the real-history-blending disclosure: a rule's threshold evaluation that counts real already-committed events alongside the pasted event discloses that real production history contributed to the result. Implemented via a read-only pre-check plus `test_blended_with_real_history_is_disclosed`.

## 3. SOAR Preview — Mac AI

- [x] 3.1 Wire the rollback-only transaction orchestrator to call `engines/playbook_engine.match_playbooks` against the simulated (uncommitted, same-transaction) alert row and capture matched playbook definitions.
- [x] 3.2 Wire response-action selection (`core/ip_helpers.determine_response_action`) to produce a selected-response preview without calling `enqueue_response_action` or any queue-insert path. Implemented by reading the `response_action` field already computed and stored on the simulated (uncommitted) alert row — `determine_response_action` itself is never called a second time by the orchestrator.
- [x] 3.3 Verify no playbook execution row, `soar_response_decisions` row, or `response_actions_queue` row is created for a simulation run (covered by the task 1.7 integration test).
- [x] 3.4 Surface approval requirements from a matched playbook's step configuration (`risk_level`/`require_approval` steps) as a preview flag without creating an approval record.
- [x] 3.5 Explicitly report "no playbook match" as a distinct SOAR preview state (not an omitted section) when no enabled playbook's trigger configuration matches.
- [x] 3.6 `core/ip_helpers.enqueue_response_action` is never called anywhere in the simulation code path in this implementation — the SOAR preview is derived entirely from the already-computed alert row and read-only playbook matching, so the "exclude response-action-queue preview detail from V1" fallback this task anticipated was the path taken; there was nothing to empirically verify because the function is never invoked.
- [x] 3.7 Add tests confirming no integration adapter (Slack, Teams, email, webhook, firewall) is invoked during a simulation run, including for a matched playbook whose steps would call one on the real path. Implemented via `test_no_integration_adapter_invoked_during_simulation`, patching the Slack adapter's outbound webhook call.

## 4. Frontend Workspace — Mac AI

- [x] 4.1 Add "Detection Simulator" to the sidebar/section configuration, visible to analyst and super-admin roles, following the existing sidebar-workspace registration pattern used by Source Health. Registered as `detection-simulator` in the `soc` group, `visibleWhen: canTakeAlertActions` (analyst or super-admin), matching Source Health's boundary.
- [x] 4.2 Add a focused simulation service module calling the new endpoint with existing session/credential and JSON/error-handling conventions. `frontend/src/services/detectionSimulatorService.js` adds `loadSimulatorRules()` and `runDetectionSimulation()`, reusing `buildSiemPath`/`parseJsonResponse`/`getApiErrorMessage`.
- [x] 4.3 Build the source selector (existing canonical source inventory), raw log/JSON paste input, existing-rule selector (sourced from the existing detection-rules listing), and Run Simulation action. Rule options come from the new `GET /detection-simulator/rules` endpoint (added this round; reuses `get_all_effective_detection_rules()` read-only, exposed to the analyst-or-super-admin boundary since `/admin/detection-rules` is super-admin only).
- [x] 4.4 Render the results area: parser output, normalized event, detection result, alert preview, MITRE preview, and SOAR preview, each reflecting the backend's per-stage response schema from task 1.1.
- [x] 4.5 Implement loading, empty, and error states for the simulation request without misrepresenting a failed request as a completed simulation.
- [x] 4.6 Add focused component/service tests: role visibility, request submission with exact selected source/input/rule, and rendering of each results section. `detectionSimulatorService.test.js` (9 tests), `DetectionSimulatorPanel.test.js` (14 tests).

## 5. Pipeline Visualization — Mac AI

- [x] 5.1 Build the pipeline-stage visualization component rendering, in exact order: Raw Input, Parser, Normalized Event, Detection Applicability, Detection Evaluation, Threshold/Window Evaluation, Alert Preview, MITRE Mapping, SOAR Preview. `DetectionSimulatorPipeline.js`.
- [x] 5.2 Implement succeeded/skipped/failed visual states per stage with accessible text labels (not color-only indicators). Each stage renders a symbol + text label + `aria-label`, not color alone.
- [x] 5.3 Wire early-stage failures (e.g., parser failure) to visually mark downstream stages as skipped with a reason referencing the upstream failure.
- [x] 5.4 Add tests asserting exact stage order and correct success/skipped/failed rendering for representative backend responses (full success, early failure, applicability failure, threshold-not-met). `DetectionSimulatorPipeline.test.js` (6 tests).

## 6. Explainability — Mac AI

- [x] 6.1 **Resolved.** Renders the numeric near-miss/threshold explanation (observed value vs. configured threshold, plus the evaluated window) produced by task 2.6's evidence-call instrumentation, in the Threshold/Window Evaluation stage's explanation card. When evidence is unavailable (dedup-suppressed case), this is disclosed explicitly rather than showing a fabricated or missing-without-explanation number.
- [x] 6.2 Render existing-open-alert suppression reasoning (task 2.8) distinctly from a plain non-match in the Alert Preview / Threshold-Window stage.
- [x] 6.3 Render real-production-history disclosure (task 2.9) so an analyst can distinguish a result driven solely by pasted input from one blended with real history.
- [x] 6.4 Render selected-playbook and approval-required reasoning (tasks 3.4–3.5) in the SOAR Preview stage.
- [x] 6.5 Add end-to-end tests (backend response fixtures) covering each explainability example named in the proposal: parser failure, normalization failure, rule not applicable, threshold reached/not reached, existing alert suppression, detection matched, selected playbook, approval required. `DetectionSimulatorExplainability.test.js` (12 tests).

## 7. Verification & Deployment — Mac AI / VM AI

- [x] 7.1 (Mac AI) Run the full backend focused test suite for the simulation endpoint, detection-engine/correlation-engine regression tests, and the zero-durable-write integration test; record commands and results in `verification.md`. 39 detection-simulator tests, 303 targeted regression tests, 1812/1815 full-suite tests (3 pre-existing, confirmed-unrelated failures) all passed.
- [x] 7.2 (Mac AI) Run the full frontend focused and full test suites (`--runInBand --watchAll=false`) covering the new workspace, sidebar registration, and existing-workspace regression protection; record results in `verification.md`. 813/813 passed.
- [x] 7.3 (Mac AI) Run the frontend production build and record any warnings. Passed; only 3 pre-existing warnings, none in Detection Simulator files.
- [x] 7.4 (Mac AI) Perform local browser verification: sidebar visibility by role, full pipeline-visualization rendering for success/skip/fail states, explainability presentation, dark theme/contrast, keyboard/accessibility, and confirm no console errors; capture evidence paths in `verification.md`. Completed against a real local Postgres-backed backend; see `verification.md` for the full scenario list and the one disclosed local-environment limitation (pre-existing DB schema drift, unrelated to this change).
- [x] 7.5 (Mac AI) Run `git diff --check` and `openspec validate add-detection-simulator-workspace --strict`; record results. Both passed.
- [x] 7.6 (Mac AI) Review the final diff to confirm no unintended changes to production `/ingest` behavior, detection-engine alerting behavior, schema, secrets, environment, or service units. Confirmed: `git diff --stat` shows zero changes to `engines/detection_engine.py`, `engines/correlation_engine.py`, `routes/ingest_routes.py`, `schema.sql`, any migration, or any service unit; `siem_backend.py` has exactly 2 added lines (blueprint import + registration).
- [x] 7.7 (Mac AI) Prepare a VM deployment handoff section in `verification.md` with the approved commit placeholder, expected backend/frontend files, no-migration expectation (or reviewed exception), read-only production verification steps, rollback plan, and explicit next owner; do not commit or push without user authorization. Done — see "Phase 7 VM Deployment Handoff" in `verification.md`.
- [ ] 7.8 (VM AI) After explicit deployment authorization, confirm a clean VM worktree, fetch without merging, verify the approved commit, and sync only via the existing source-of-truth deployment policy.
- [ ] 7.9 (VM AI) Deploy backend first, verify `/health` and an authenticated read-only simulation-endpoint smoke test (a benign pasted example, still zero-durable-write by design), then deploy the Mac-built frontend artifact.
- [ ] 7.10 (VM AI) Verify the deployed workspace in production: sidebar visibility by role, a full simulation run against a benign example, pipeline visualization, and confirm zero row-count change in the tables listed in task 1.7 using read-only queries only.
- [ ] 7.11 (VM AI) Record deployed commit, service/artifact actions, sanitized results, production evidence, clean-tree post-check, and rollback readiness in `verification.md` without modifying source on the VM.
